# =========================================================
# REQUIRED AI SERVICE API KEYS
# =========================================================
# API Key for Deepgram (Speech-to-Text)
DEEPGRAM_API_KEY="" 
# API Key for ElevenLabs (Text-to-Speech)
ELEVENLABS_API_KEY=""
# API Key for OpenRouter (LLM)
OPENROUTER_API_KEY=""

# =========================================================
# REQUIRED DATABASE (SUPABASE) CONFIGURATION
# =========================================================
# The URL for your Supabase project (used for connection)
SUPABASE_URL=""
# The 'anon public' key for client-side/read-write access
SUPABASE_ANON_KEY=""

# =========================================================
# REQUIRED TELEPHONY & AUTHENTICATION
# =========================================================
# Your Twilio Account SID
TWILIO_ACCOUNT_SID=""
# Your Twilio Auth Token
TWILIO_AUTH_TOKEN=""
# Path to your downloaded Google Service Account JSON key file (GLOBAL FALLBACK)
# See docs/GOOGLE_CALENDAR_SETUP.md for detailed setup instructions
# This is now optional - clients can use OAuth or upload their own service accounts
GOOGLE_SERVICE_ACCOUNT_FILE_PATH=""
# The UUID of the client configuration row in the 'clients' DB table
CLIENT_ID=""

# =========================================================
# GOOGLE CALENDAR AUTHENTICATION (PER-CLIENT)
# =========================================================
# Google OAuth 2.0 Client ID for per-client calendar authentication
# Create these credentials in Google Cloud Console > APIs & Services > Credentials
# See docs/GOOGLE_CALENDAR_SETUP.md for setup instructions
GOOGLE_OAUTH_CLIENT_ID=""
# Google OAuth 2.0 Client Secret
GOOGLE_OAUTH_CLIENT_SECRET=""

# Encryption key for calendar credentials (REQUIRED for security)
# Generate a secure 32-byte hex key with: openssl rand -hex 32
# IMPORTANT: Keep this secret safe and never commit to version control
CALENDAR_CREDENTIALS_ENCRYPTION_KEY=""

# Base URL for OAuth callbacks (e.g., http://localhost:8000 or https://yourdomain.com)
# Must match the authorized redirect URIs in your Google OAuth app
BASE_URL="http://localhost:8000"

# =========================================================
# Stripe Billing
# =========================================================
STRIPE_PUBLISHABLE_KEY=""
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

